# Application
spring.application.name=lesson-service
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# Server
server.port=${SERVER_PORT:8082}
server.compression.enabled=true
server.error.include-message=always
server.error.include-binding-errors=always

# MongoDB
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/lesson}
spring.data.mongodb.database=${MONGODB_DATABASE:lesson}
spring.data.mongodb.auto-index-creation=true

# Spring Cloud Stream - Kafka
spring.cloud.stream.kafka.binder.brokers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

# Function bindings for Kafka consumers
spring.cloud.function.definition=userRegisteredListener;learningProfileCreatedListener

# Input binding for userRegisteredListener
spring.cloud.stream.bindings.userRegisteredListener-in-0.destination=user.events
spring.cloud.stream.bindings.userRegisteredListener-in-0.group=lesson-service
spring.cloud.stream.bindings.userRegisteredListener-in-0.consumer.max-attempts=3
spring.cloud.stream.bindings.userRegisteredListener-in-0.consumer.back-off-initial-interval=1000
spring.cloud.stream.bindings.userRegisteredListener-in-0.consumer.back-off-multiplier=2.0
spring.cloud.stream.kafka.bindings.userRegisteredListener-in-0.consumer.enable-dlq=true
spring.cloud.stream.kafka.bindings.userRegisteredListener-in-0.consumer.dlq-name=user.events.lesson-service.dlq

# Input binding for learningProfileCreatedListener
spring.cloud.stream.bindings.learningProfileCreatedListener-in-0.destination=user.events
spring.cloud.stream.bindings.learningProfileCreatedListener-in-0.group=lesson-service
spring.cloud.stream.bindings.learningProfileCreatedListener-in-0.consumer.max-attempts=3
spring.cloud.stream.kafka.bindings.learningProfileCreatedListener-in-0.consumer.enable-dlq=true

# Output binding for lesson events
spring.cloud.stream.bindings.lessonEvents-out-0.destination=lesson.events
spring.cloud.stream.kafka.bindings.lessonEvents-out-0.producer.message-key-expression=headers['partitionKey']

# Security - OAuth2 Resource Server (Keycloak)
spring.security.oauth2.resourceserver.jwt.issuer-uri=${KEYCLOAK_ISSUER_URI:http://localhost:8080/realms/wespeak}
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${KEYCLOAK_JWK_SET_URI:http://localhost:8080/realms/wespeak/protocol/openid-connect/certs}

# Security toggle - set to false to disable security (dev mode)
app.security.enabled=${SECURITY_ENABLED:true}

# Actuator
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoints.web.base-path=/actuator
management.endpoint.health.show-details=when-authorized
management.endpoint.health.probes.enabled=true
management.health.mongo.enabled=true
management.metrics.export.prometheus.enabled=true
management.metrics.tags.application=${spring.application.name}

# OpenAPI Documentation
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.operations-sorter=method
springdoc.swagger-ui.tags-sorter=alpha
springdoc.show-actuator=false

# Logging
logging.level.root=${LOG_LEVEL:INFO}
logging.level.org.wespeak=${LOG_LEVEL:INFO}
logging.level.org.springframework.data.mongodb=${LOG_LEVEL:INFO}
logging.level.org.springframework.cloud.stream=${LOG_LEVEL:INFO}
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# Application Custom Configuration
app.name=${spring.application.name}
app.version=1.0.0
app.description=WeSpeak Lesson Service - Curriculum and Progress Management
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:4200,http://localhost:3000}
