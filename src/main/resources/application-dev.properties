# Dev environment overrides
spring.data.mongodb.uri=mongodb://localhost:27017/conversation-dev

# Disable security in dev mode by default
app.security.enabled=${SECURITY_ENABLED:false}

# Enable debug logging for development
logging.level.org.wespeak=DEBUG

# Mock S3 in dev (use local storage)
aws.s3.mock-enabled=true
