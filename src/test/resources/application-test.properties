# Test environment
spring.data.mongodb.uri=mongodb://localhost:27017/conversation-test

# Disable security in tests
app.security.enabled=false

# Reduced logging
logging.level.org.springframework=WARN
logging.level.org.wespeak=DEBUG

# Mock S3
aws.s3.mock-enabled=true
